actor: BaseTester
modules:
  enabled:
    - Asserts
    - \Helper\Base
    - \Helper\Data
    - Redis:
        host: m2t_redis
        port: 6379
        database: 1
        cleanupBefore: suite
  step_decorators: ~
